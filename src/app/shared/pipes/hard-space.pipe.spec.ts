import { HardSpacePipe } from './hard-space.pipe';

describe('HardSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new HardSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
